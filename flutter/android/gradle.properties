org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin incremental caches cannot relativize sources when the project is on
# F: and the Pub cache is on C:. A deterministic in-process release build is
# preferable to a daemon failure on Windows multi-drive workspaces.
kotlin.incremental=false
kotlin.compiler.execution.strategy=in-process
